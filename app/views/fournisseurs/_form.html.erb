<h1 class="text-2xl font-bold text-center my-6">
  <%= @fournisseur.new_record? ? "Créer un Fournisseur" : "Modifier le Fournisseur" %>
</h1>

<!-- 🚀 Affichage des erreurs -->
<% if @fournisseur.errors.any? %>
  <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4">
    <strong>Attention !</strong> Il y a <%= @fournisseur.errors.count %> erreur(s) :
    <ul class="mt-2">
      <% @fournisseur.errors.full_messages.each do |message| %>
        <li class="ml-4">• <%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with(model: @fournisseur, local: true) do |form| %>
  <!-- Nom -->
  <div class="mb-4">
    <%= form.label :nom, "Nom du Fournisseur", class: "block text-sm font-medium text-gray-700" %>
    <%= form.text_field :nom, class: "w-full p-2 border rounded", required: true %>
  </div>

  <!-- Email -->
  <div class="mb-4">
    <%= form.label :email, "Email", class: "block text-sm font-medium text-gray-700" %>
    <%= form.email_field :email, class: "w-full p-2 border rounded" %>
  </div>

  <!-- Téléphone -->
  <div class="mb-4">
    <%= form.label :telephone, "Téléphone", class: "block text-sm font-medium text-gray-700" %>
    <%= form.text_field :telephone, class: "w-full p-2 border rounded" %>
  </div>

  <!-- Adresse -->
  <div class="mb-4">
    <%= form.label :adresse, "Adresse", class: "block text-sm font-medium text-gray-700" %>
    <%= form.text_area :adresse, class: "w-full p-2 border rounded" %>
  </div>

  <!-- Type de fournisseur -->
  <div class="mb-4">
    <%= form.label :type_fournisseur, "Type de Fournisseur", class: "block text-sm font-medium text-gray-700" %>
    <%= form.select :type_fournisseur, 
                    ["Société (Neuf)", "Particulier / Organisme (Occasion)"], 
                    {}, 
                    class: "w-full p-2 border rounded" %>
  </div>

  <!-- Boutons -->
  <div class="mt-4 flex space-x-2">
    <%= link_to "Annuler", fournisseurs_path, class: "px-4 py-2 bg-gray-300 rounded hover:bg-gray-400" %>
    <%= form.submit "Enregistrer", class: "px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600" %>
  </div>
<% end %>
