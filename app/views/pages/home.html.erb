<div class="text-center">
  <h2 class="text-2xl font-bold">Bienvenue dans Boutique</h2>
  <p class="text-gray-700 mt-4">Gérez vos produits neufs, d’occasion et en dépôt-vente facilement.</p>
</div>

<%#
  <turbo-frame id="turbo_frame_1">
    <!-- Le bouton envoie une requête GET vers l'action qui fournira le contenu -->
    <%= button_to 'Charger le texte', texte_path, method: :get, data: { turbo_frame: "turbo_frame_2" } %>
  </turbo-frame>

  <turbo-frame id="turbo_frame_2">
    <!-- Ce turbo frame est initialement vide et sera mis à jour par la réponse -->
  </turbo-frame>
