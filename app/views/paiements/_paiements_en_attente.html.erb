<% if paiements_en_attente.any? %>
  
  <table class="table-auto w-full border border-gray-300 shadow-sm rounded-lg overflow-hidden">
    <thead class="bg-gray-100 text-left">
      <tr>
        <th class="px-4 py-3">Déposante</th>
        <th class="px-4 py-3 text-right">Montant à verser</th>
        <th class="px-4 py-3 text-center">Action</th>
      </tr>
    </thead>
    <tbody>
      <% paiements_en_attente.each do |paiement| %>
        <tr class="border-t hover:bg-gray-50 transition">
          <td class="px-4 py-3 font-medium text-gray-800">
            <%= paiement[:client].prenom %> <%= paiement[:client].nom %>
          </td>
          <td class="px-4 py-3 text-right text-green-700 font-semibold">
            <%= number_to_currency(paiement[:total]) %>
          </td>
          <td class="px-4 py-3 text-center">
            <%= link_to new_client_versement_path(paiement[:client]),
              class: "inline-flex items-center px-3 py-1 bg-blue-600 text-white text-sm rounded hover:bg-blue-700 transition" do %>
              💰 Payer
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p class="italic text-green-700 mt-4">Aucun versement en attente.</p>
<% end %>
