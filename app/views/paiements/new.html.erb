<div class="max-w-xl mx-auto p-6 bg-white rounded-xl shadow-md">
  <h1 class="text-2xl font-bold mb-6">💰 Enregistrer un Paiement pour <%= @client.nom %> <%= @client.prenom %></h1>

  <%= form_with(model: [@client, @paiement], class: "space-y-4") do |f| %>
    
    <div>
      <%= f.label :montant, "Montant versé (€)", class: "block text-sm font-medium text-gray-700" %>
      <%= f.number_field :montant, step: 0.01, value: @paiement.montant || 0, disabled: true, class: "mt-1 block w-full border border-gray-300 rounded-md shadow-sm px-3 py-2 bg-gray-100" %>
    </div>

    <div>
      <%= f.label :methode_paiement, "Méthode de paiement", class: "block text-sm font-medium text-gray-700" %>
      <%= f.select :methode_paiement, ["espèces", "chèque", "virement", "carte bancaire"], {}, class: "mt-1 block w-full border border-gray-300 rounded-md shadow-sm px-3 py-2" %>
    </div>

    <div>
      <%= f.label :date_paiement, "Date du paiement", class: "block text-sm font-medium text-gray-700" %>
      <%= f.date_field :date_paiement, value: Date.today, disabled: true, class: "mt-1 block w-full border border-gray-300 rounded-md shadow-sm px-3 py-2 bg-gray-100" %>
    </div>

    <div>
      <%= f.submit "✅ Valider le paiement", class: "bg-green-600 hover:bg-green-700 text-black font-semibold py-2 px-6 rounded shadow inline-block" %>
    </div>

  <% end %>
</div>
