<div class="bg-white shadow-lg rounded-lg overflow-hidden">
  <div class="p-4">
    <h2 class="text-lg font-semibold"><%= produit.nom %></h2>
    <p class="text-gray-500 text-sm"><%= truncate(produit.description, length: 50) %></p>
    <p class="text-blue-600 font-bold mt-2"><%= produit.prix %> €</p>

    <div class="flex justify-end mt-4">
      <%= link_to "Détails", produit_path(produit), class: "text-blue-500 hover:underline" %>
    </div>
  </div>
</div>
