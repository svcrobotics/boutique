<!-- app/views/ventes/new.html.erb sans Stimulus -->
<div class="max-w-3xl mx-auto bg-white shadow rounded p-6">
  <h1 class="text-2xl font-semibold mb-6">🛒 Encaissement</h1>

  <!-- FORMULAIRE CODE-BARRES (redirige en HTML) -->
  <%= form_with url: recherche_produit_ventes_path, method: :post, local: true do |f| %>
    <div class="mb-4">
      <%= f.label :code_barre, "Code-barres" %>
      <%= f.text_field :code_barre, autofocus: true, autocomplete: "off", class: "w-full rounded border-gray-300" %>
    </div>
  <% end %>

  <!-- TABLEAU DES PRODUITS SCANNÉS -->
  <% if @produits.any? %>
    <table class="table-auto w-full mb-6 border">
      <thead>
        <tr class="bg-gray-100">
          <th class="px-4 py-2">Produit</th>
          <th class="px-4 py-2">Quantité</th>
          <th class="px-4 py-2">Prix</th>
          <th class="px-4 py-2">Total</th>
          <th class="px-4 py-2">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @produits.each do |id, produit| %>
          <% quantite = @quantites[id] %>
          <tr class="border-t">
            <td class="px-4 py-2"><%= produit.nom %></td>
            <td class="px-4 py-2">
              <%= form_with url: modifier_quantite_ventes_path, method: :post, local: true do |f| %>
                <%= hidden_field_tag :produit_id, produit.id %>
                <%= number_field_tag :quantite, quantite, min: 1, class: "w-16 border rounded" %>
                <%= submit_tag "OK", class: "ml-2 px-2 py-1 bg-blue-500 text-white rounded" %>
              <% end %>
            </td>
            <td class="px-4 py-2"><%= number_to_currency(produit.prix) %></td>
            <td class="px-4 py-2"><%= number_to_currency(produit.prix * quantite) %></td>
            <td class="px-4 py-2">
              <%= button_to "Retirer", retirer_produit_ventes_path(produit_id: produit.id), method: :post, class: "text-red-600" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p class="text-gray-500 italic mb-6">Aucun produit pour le moment.</p>
  <% end %>

  <!-- FORMULAIRE ENCAISSEMENT -->
  <%= form_with url: ventes_path, method: :post, local: true do %>
    <div class="mb-4">
      <label for="client_nom" class="block font-medium text-gray-700">Cliente</label>
      <%= text_field_tag :client_nom, params[:client_nom], list: "clients_list",
            class: "w-full rounded border-gray-300 shadow-sm",
            placeholder: "Nom de la cliente (ou vide pour sans cliente)" %>

      <datalist id="clients_list">
        <% Client.order(:nom).pluck(:nom).uniq.each do |nom| %>
          <option value="<%= nom %>">
        <% end %>
      </datalist>
    </div>

    <div class="mb-4">
      <label for="mode_paiement" class="block font-medium text-gray-700">Mode de paiement</label>
      <%= select_tag :mode_paiement, options_for_select(["Espèces", "Carte bancaire", "AMEX", "Chèque"], params[:mode_paiement]),
          prompt: "Sélectionner un mode de paiement",
          class: "w-full rounded border-gray-300 shadow-sm" %>
    </div>

    <div class="text-right">
      <button type="submit" class="w-full bg-green-600 text-white font-semibold text-lg py-3 rounded hover:bg-green-700">
        💳 Encaisser maintenant
      </button>
    </div>
  <% end %>
</div>
